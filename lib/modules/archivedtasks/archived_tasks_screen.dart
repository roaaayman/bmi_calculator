import 'package:flutter/material.dart';

class archivedTasks extends StatelessWidget {
  const archivedTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('ARCHIVED'));
  }
}
